<Window x:Class="WpfDataGRidText.WinDBInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDataGRidText"
        mc:Ignorable="d"
        Title="WinDBInfo" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="dataGridUser" AutoGenerateColumns="False" ItemsSource="{Binding}">
            <DataGrid.Columns>
            <DataGridTextColumn Header="Id" Binding="{Binding id}"/>
                <DataGridTextColumn Header="Id" Binding="{Binding id}"/>
                <DataGridTextColumn Header="用户名" Binding="{Binding UserId}"/>
                <DataGridTextColumn Header="密码" Binding="{Binding UserPwd}"/>
                <DataGridTextColumn Header="真实姓名" Binding="{Binding TrueName}"/>
                <DataGridTextColumn Header="性别" Binding="{Binding Gender}"/>
                <DataGridTextColumn Header="出生年月" Binding="{Binding Birth}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
